services:
  postgres:
    image: postgres:16
    restart: always
    environment:
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=root
      - POSTGRES_DB=online_shopping
    ports:
      - "5428:5432"
    volumes:
      - db:/var/lib/postgresql

volumes:
  db:
    driver: local
